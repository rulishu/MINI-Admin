import OrderAdd from './OrderAdd';
import OrderList from './OrderList';

const OriderAdd = () => {
  return (
    <div>
      <OrderAdd onAdd={() => {}} />
      <OrderList />
    </div>
  );
};

export default OriderAdd;
